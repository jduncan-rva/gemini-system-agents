description = "List all available system agents with descriptions"
prompt = """
First, read the agent registry file at ${extensionPath}/system-agents/agents.json.

Task: List all available system agents from the provided JSON context.
Format the output as a clean, readable list.
For each agent, show:
- **ID** (for commands)
- **Name**
- **Category**
- **Description**
"""

