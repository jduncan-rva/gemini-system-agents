description = "Show detailed information about a specific agent"
prompt = """
First, read the system agent registry file using `run_shell_command` with `cat ${extensionPath}/system-agents/agents.json`. **Do not use `read_file`.**

Then, attempt to read the custom agent registry file using `run_shell_command` with `cat ${extensionPath}/custom/agents.json`.

Task: Find the agent with ID '{{args}}' by searching both registries.
Display the full details for this agent, including:
- Name & Category
- Description
- Personality
- Use Cases (bullet points)
- **Switch Command**: `export GEMINI_SYSTEM_MD=~/.gemini/extensions/system-agents/system-agents/<file>` (Adjust path for custom agents if applicable)
"""

